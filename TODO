(%100)udp发包ack确认，失败自动重发，发包顺序简单校验
(%100)客户端穿透失败自动换向重连
(%50)服务端管理端口,广播,踢人,关服,限制人数,限制ip,限制服务名
(%100)版本校验
(%100)udp穿透失败后切换成c/s模式
(%0)打洞过程不依赖服务端会话
(%0)账号数据存储，登录认证，登录次数，流量，时长限制(go-sql-driver)
(%0)GUI支持(termbox)(show after log)
(%0)文件传输，浏览，简单shell功能
(%0)nat类型检测
(%0)新的分级日志(factorlog)

